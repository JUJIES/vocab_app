{
  "name": "lerndeck",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check teacher.js && node --check pwa.js && node --check ui-motion.js && node --check irregular-verbs.js && node --check server.js && node --check lib/visual-service.js && node --check sw.js && node --check scripts/provision-teachers.js",
    "provision:teachers": "node scripts/provision-teachers.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.21.2",
    "openai": "^5.23.2",
    "sharp": "^0.35.4"
  },
  "overrides": {
    "qs": "6.16.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
